<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.SpringBoot_movie_admin.mapper.AdminMapper">
   <!-- &lt;!&ndash;登录查询&ndash;&gt;
    <select id="adminLogin" resultType="com.example.SpringBoot_movie_admin.entity.MovieAdmin">
        select *
        from movie_admin
        where userName=#{userName} and userPassword=#{userPassword}
    </select>

    &lt;!&ndash;添加用户&ndash;&gt;
    <insert id="addUser" parameterType="com.example.SpringBoot_movie_admin.entity.MovieAdmin">
        insert
        into movie_admin(userName,userPassword,email,mobile)
        values (#{userName},#{userPassword},#{email},#{mobile})
    </insert>
    &lt;!&ndash;删除用户&ndash;&gt;
    <delete id="delUser" parameterType="int">
        delete from movie_admin where id=#{id}
    </delete>-->


</mapper>
